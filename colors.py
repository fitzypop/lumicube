import random
import threading

# must code and run in lumicube editor

colors = [blue, cyan, green, grey, magenta, orange, pink, purple, red, white, yellow]
all_colors = [black, *colors]


while True:
    speaker.say("sup dawg")
    display.scroll_text("Hello LumiCube!", random.choice(colors), black, 0.9)
